<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.materialdesign">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".activity.ProfessorFragment" />
        <activity
            android:name=".activity.LogoActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.login_doctor"
            android:label="@string/title_activity_test_as_doctor" />
        <activity
            android:name=".activity.create_account_doctor"
            android:label="@string/title_activity_create_account_doctor" />
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/title_activity_first" />
        <activity
            android:name=".activity.ActivityNotification"
            android:theme="@style/notificationStyle" />
        <activity
            android:name=".activity.Activity_Date"
            android:label="@string/title_activity_activity__date"
            android:theme="@style/calendarStyle" />
        <activity
            android:name=".activity.ScheduleActivity"
            android:label="@string/title_activity_scedual"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.StudentSignUp" />
        <activity android:name=".activity.StudentTabs" />
        <activity android:name=".activity.ActivityDetails"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

    </application>

</manifest>
